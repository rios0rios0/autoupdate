kind: 'Fixed'
body: 'fixed the pull request checklist, which told contributors to run `go test`: every test file in this repository carries the `unit` build tag, so a plain `go test ./...` runs zero tests — the checklist now points at `make lint`, `make test` and `make sast`, matching `CONTRIBUTING.md`'
time: '2026-08-25T22:38:02.003974198Z'
